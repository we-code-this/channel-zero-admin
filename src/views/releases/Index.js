import React from "react";
import Helmet from "react-helmet";

const Index = () => (
  <div>
    <Helmet>
      <title>Releases</title>
    </Helmet>
    <p>Releases</p>
  </div>
);

export default Index;
