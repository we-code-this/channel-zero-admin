import React from "react";
import Helmet from "react-helmet";

const Dashboard = () => (
  <div>
    <Helmet>
      <title>Dashboard</title>
    </Helmet>
    <p>Dashboard</p>
  </div>
);

export default Dashboard;
