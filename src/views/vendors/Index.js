import React from "react";
import Helmet from "react-helmet";

const Index = () => (
  <div>
    <Helmet>
      <title>Vendors</title>
    </Helmet>
    <p>Vendors</p>
  </div>
);

export default Index;
